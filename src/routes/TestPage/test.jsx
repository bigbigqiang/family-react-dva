import React, { PureComponent } from 'react';
import { Calendar, Button, Carousel } from 'antd';
import { Pie } from '../../components/Charts'
import moment from 'moment';
import lodash from 'lodash';
import G2 from 'g2';

export default class calander extends PureComponent {
    render() {
        return (
            <div>
                {/* <Pie></Pie> */}
            </div>
        )
    }
}